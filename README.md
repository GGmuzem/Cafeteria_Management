# 🍔 Cafeteria Management System

![Python](https://img.shields.io/badge/python-3.9+-blue.svg)
![Flask](https://img.shields.io/badge/flask-3.x-green.svg)
![Docker](https://img.shields.io/badge/docker-ready-blue.svg)
![License](https://img.shields.io/badge/license-MIT-lightgrey.svg)

## 📖 Описание проекта

**Cafeteria Management System** — это веб-приложение для автоматизации работы столовой (школьной или корпоративной). Система связывает учеников, поваров и администраторов в едином интерфейсе, позволяя управлять заказами, складскими запасами, меню и финансами.

Приложение решает проблемы очередей, учета продуктов и контроля расходов, предоставляя прозрачную систему биллинга и складского учета.

## ✨ Основной функционал

### 👨‍🎓 Для Студента (Student)
* **Электронное меню:** Просмотр актуального меню с ценами, составом и весом блюд.
* **Умный фильтр:** Фильтрация блюд по аллергенам (скрытие опасных позиций).
* **Заказ питания:** Покупка завтраков и обедов с автоматическим списанием средств с внутреннего кошелька.
    * *Ограничение:* Система контролирует, чтобы студент мог заказать только 1 завтрак и 1 обед в день.
* **Отзывы:** Возможность оставлять оценки и комментарии к блюдам.
* **Личный кабинет:** Просмотр истории транзакций, управление настройками (аллергены, предпочтения).

### 👨‍🍳 Для Повара (Cook)
* **Управление меню:** Добавление, редактирование и удаление блюд (название, цена, состав, тип приема пищи).
* **Работа со складом:** Просмотр остатков продуктов.
* **Заявки на закупку:** Создание заявок на недостающие продукты для администратора.
* **История:** Просмотр истории заказов.

### 👮‍♂️ Для Администратора (Admin)
* **Управление пользователями:** Смена ролей (Student, Cook, Admin), удаление пользователей.
* **Управление складом:** Добавление товаров, списание, пополнение запасов.
* **Обработка заявок:** Одобрение, отклонение или редактирование заявок на закупку продуктов от поваров.
* **Финансы и статистика:** Просмотр полной истории операций и посещаемости.

## 🛠 Технологический стек

* **Язык:** Python 3
* **Фреймворк:** Flask
* **База данных:** SQLite (через SQLAlchemy)
* **Аутентификация:** Flask-Login
* **Валидация данных:** Pydantic (в модуле `service.py`)
* **Контейнеризация:** Docker & Docker Compose

## ⚙️ Настройка окружения

Перед запуском проекта необходимо создать файл `.env` в корне проекта. Вы можете использовать `.env.example` в качестве шаблона.

1.  Скопируйте пример файла конфигурации:
    ```bash
    cp .env.example .env
    ```
2.  Откройте файл `.env` и заполните необходимые переменные:
    *   `KEY`: Секретный ключ приложения, пример: '3df5tPHi4nZQhof7gCKGPKOOy3z_HJEXmQNie1i55_k=' (для сессий и безопасности).
    *   `SMTP_EMAIL`: Почта для отправки уведомлений.
    *   `SMTP_PASSWORD`: Пароль приложения для почты (возможно потребуетя app password с двухфакторной аутентификацией).
    *   `SMTP_SERVER`: Адрес SMTP сервера (например, smtp.gmail.com).
    *   `SMTP_PORT`: Порт SMTP сервера (обычно 587).

## 🚀 Установка и запуск

Проект поддерживает запуск через Docker, что является рекомендуемым способом.

### Вариант 1: Запуск через Docker (Рекомендуется)

*Ссылка для скачивания: https://www.docker.com/products/docker-desktop*

#### ВНИМАНИЕ: Для этого способа вам нужен скачанный Docker

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/GGmuzem/Cafeteria_Management.git
    cd Cafeteria_Management
    ```

3.  **Запустите контейнеры:**
    ```bash
    docker compose up -d --build
    ```

5.  Приложение будет доступно по адресу: `http://localhost:8080`

### Вариант 2: Ручной запуск (без Docker)

*ВНИМАНИЕ: при таком запуске не будут работать уведомления пользователям*

1.  **Создайте виртуальное окружение:**
    ```bash
    python -m venv venv
    source venv/bin/activate  # Для Linux/macOS
    venv\Scripts\activate     # Для Windows
    ```

    *Если выдает ошибку "выполнение сценариев отключено в этой  
системе", то введите в powershell: Set-ExecutionPolicy RemoteSigned и повторите команду активации виртуального окружения*

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Запустите приложение:**
    ```bash
    python app.py
    ```
    *При первом запуске база данных будет создана автоматически, также будет создан администратор по умолчанию.*

4. Приложение будет доступно по адресу: `http://localhost:5000`

## 📂 Структура проекта

```text
Cafeteria_Management/
├── admin.py            # Логика панели администратора (заявки, роли)
├── app.py              # Точка входа, настройки App и Auth
├── cook.py             # Логика панели повара (меню, заявки)
├── service.py          # Бизнес-логика (склад, покупки, транзакции)
├── student.py          # Логика студента (заказы, отзывы, фильтры)
├── config.py           # Конфигурация приложения и БД
├── requirements.txt    # Зависимости Python
├── Dockerfile          # Инструкция для сборки образа
├── docker-compose.yml  # Оркестрация контейнеров
├── database/           # Модели базы данных (ORM)
│   ├── users.py
│   ├── menu.py
│   ├── store.py
│   ├── requests.py
│   ├── history.py
│   └── ...
└── templates/          # HTML шаблоны (Jinja2)
